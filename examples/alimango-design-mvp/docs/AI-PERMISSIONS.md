# Permissions and project access

The MVP separates connection-level and project-level access.

Connection permissions:
- Read shared projects
- Create new projects
- Edit shared projects
- Create client preview links (off by default)
- Request export handoff (off by default)

Existing project access defaults to **off**. The user must explicitly share each project with the current AI connection.
